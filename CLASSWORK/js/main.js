// document.querySelector(".btn").onclick = function () {
//   document.querySelector(".text").style.display = "none";
// };

var firstName = prompt("Enter the firstname");

var lastName = prompt("Enter the lastname");

var middleName = prompt("Enter the middlename");

var fullName = firstName + " " + lastName + " " + middleName + " og'li";

// document.querySelector(".full-name").innerHTML = fullName;
// document.write(fullName);

// window.alert(fullName);
// window.confirm(fullName);

console.log(fullName);

// variable names

// a1;
// b2;
// first_name;
// $query;

// first-group;
// first*group;
// 1name;
// Firstname;
